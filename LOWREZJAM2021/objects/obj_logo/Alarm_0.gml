/// @desc Zoom in / Move up

image_xscale += zoom;
image_yscale += zoom;

//image_xscale = lerp(image_xscale, 61, 0.004);
//image_yscale = lerp(image_yscale, 61, 0.004);

show_credits = true;

alarm[0] = 1;